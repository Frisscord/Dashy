# Dashy - Python Discord Dashboard
# Setup
- pip install quart quart-discord
- Add redirects (callback, dashboard etc)
- Install Code Runner Extension
- Click the Run Button and go into localhost/5000 (default)
